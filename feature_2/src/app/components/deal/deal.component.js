var deal = {
    templateUrl: './deal.html'
};

angular
    .module('deal')
    .component('deal', deal)
    .config(function ($stateProvider) {
		$stateProvider
			.state('dealList.deal', {
				
				
			});
	});